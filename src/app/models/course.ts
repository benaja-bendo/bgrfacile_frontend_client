export interface Course {
  id ?: number
  name ?: string
  slug ?: string
  description ?: string
  path_image ?: string
  is_actif ?: string
  contents ?: any[]
  cycles ?: any[]
  levels ?: any[]
  matieres ?: any[]
  created_at ?: string
  updated_at ?: string
}
